<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>MovieMate Booking Confirmation</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      line-height: 1.6;
      color: #333;
      margin: 0;
      padding: 0;
      background-color: #f9f9f9;
    }
    .email-container {
      max-width: 600px;
      margin: 0 auto;
      background-color: #ffffff;
      border-radius: 8px;
      overflow: hidden;
      box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
    }
    .email-header {
      background-color: #FBC700;
      padding: 20px;
      text-align: center;
      color: #fff;
    }
    .email-body {
      padding: 30px;
    }
    .section {
      margin-bottom: 25px;
      padding-bottom: 15px;
      border-bottom: 1px solid #eee;
    }
    .section:last-child {
      border-bottom: none;
    }
    .section-title {
      font-size: 18px;
      font-weight: bold;
      margin-bottom: 15px;
      color: #333;
    }
    .detail-row {
      margin-bottom: 8px;
    }
    .detail-label {
      font-weight: bold;
      color: #666;
    }
    .seats-container {
      display: flex;
      flex-wrap: wrap;
      margin-top: 10px;
    }
    .seat-badge {
      display: inline-block;
      padding: 5px 10px;
      margin-right: 5px;
      margin-bottom: 5px;
      border-radius: 15px;
      background-color: #fff5d7;
      color: #e3b400;
      font-weight: bold;
    }
    .menu-item {
      display: flex;
      justify-content: space-between;
      background-color: #f8f8f8;
      padding: 8px 12px;
      border-radius: 6px;
      margin-bottom: 8px;
    }
    .quantity {
      display: inline-block;
      height: 20px;
      width: 20px;
      line-height: 20px;
      text-align: center;
      background-color: #e3b400;
      color: white;
      border-radius: 50%;
      margin-right: 8px;
    }
    .total-section {
      background-color: #fff5d7;
      padding: 15px;
      border-radius: 8px;
      margin-top: 25px;
    }
    .total-row {
      display: flex;
      justify-content: space-between;
      font-weight: bold;
    }
    .price {
      color: #e3b400;
    }
    .points-row {
      display: flex;
      justify-content: space-between;
      margin-top: 10px;
      padding-top: 10px;
      border-top: 1px solid rgba(227, 180, 0, 0.3);
      font-size: 14px;
    }
    .points-value {
      color: #28a745;
    }
    .footer {
      text-align: center;
      padding: 20px;
      background-color: #f5f5f5;
      color: #777;
      font-size: 12px;
    }
    .thank-you {
      text-align: center;
      margin: 30px 0;
      font-size: 18px;
      color: #e3b400;
      font-weight: bold;
    }
    @media only screen and (max-width: 600px) {
      .email-body {
        padding: 20px;
      }
    }
  </style>
</head>
<body>
  <div class="email-container">
    <div class="email-header">
      <h1 style="margin: 0; font-size: 28px;">Booking Confirmation</h1>
      <p style="margin: 5px 0 0 0;">Your MovieMate ticket is ready!</p>
    </div>
    
    <div class="email-body">
      <p>Hello <%= userName %>,</p>
      <p>Thank you for your booking with MovieMate. Below are your ticket details:</p>
      
      <div class="section">
        <div class="section-title">Movie Details</div>
        <div class="detail-row">
          <span class="detail-label">Movie:</span> <%= receipt.movieTitle %>
        </div>
        <div class="detail-row">
          <span class="detail-label">Date:</span> <%= formatDate(receipt.date) %>
        </div>
        <div class="detail-row">
          <span class="detail-label">Time:</span> <%= receipt.time %>
        </div>
        <div class="detail-row">
          <span class="detail-label">Room:</span> <%= receipt.room %>
        </div>
      </div>
      
      <div class="section">
        <div class="section-title">Selected Seats</div>
        <div class="seats-container">
          <% receipt.seats.forEach(function(seat) { %>
            <span class="seat-badge"><%= seat %></span>
          <% }); %>
        </div>
      </div>
      
      <% if (receipt.menuItems && receipt.menuItems.length > 0) { %>
      <div class="section">
        <div class="section-title">Food & Beverages</div>
        <% receipt.menuItems.forEach(function(item, index) { %>
          <div class="menu-item">
            <div>
              <span class="quantity"><%= item.quantity %></span>
              <%= item.name || `Item ${index + 1}` %>
            </div>
            <div>
              Rs. <%= (item.price * item.quantity).toFixed(2) %>
            </div>
          </div>
        <% }); %>
      </div>
      <% } %>
      
      <div class="section">
        <div class="section-title">Payment Information</div>
        <div class="detail-row">
          <span class="detail-label">Payment Method:</span> eSewa
        </div>
        <div class="detail-row">
          <span class="detail-label">Booking ID:</span> #<%= receipt.bookingId %>
        </div>
      </div>
      
      <div class="total-section">
        <div class="total-row">
          <span>Total Amount:</span>
          <span class="price">Rs. <%= receipt.totalPrice.toFixed(2) %></span>
        </div>
        
        <% if (receipt.loyaltyPointsEarned > 0) { %>
        <div class="points-row">
          <span>Loyalty Points Earned:</span>
          <span class="points-value">+<%= receipt.loyaltyPointsEarned %> points</span>
        </div>
        <% } %>
      </div>
      
      <div class="thank-you">
        Thank you for choosing MovieMate!
      </div>
    </div>
    
    <div class="footer">
      <p>This is an automated email, please do not reply.</p>
      <p>&copy; <%= currentYear %> MovieMate. All rights reserved.</p>
    </div>
  </div>
</body>
</html>